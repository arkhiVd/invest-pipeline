# Synthetic screenshots

Every image in this directory is generated from fictional `ACME`, `NOVA`, and
`ZEAL` values. No production database or broker export is used.

Regenerate the screenshot with:

```bash
python scripts/render_synthetic_screenshot.py
```
